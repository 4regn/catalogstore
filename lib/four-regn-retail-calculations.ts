export const DEFAULT_RETAIL_SETTINGS = { retail_price:129, unit_cost:65, commission:30, initial_units:20, minimum_sell_through:50, scale_sell_through:75, weekly_units_target:4, pilot_days:30, max_shrinkage:2 };
export type RetailSettings = typeof DEFAULT_RETAIL_SETTINGS;
export type RetailPartner = { id:string; store_code:string; name:string; location:string; contact_name:string; joined_on:string; status:string; notes:string; created_at:string; updated_at:string; stockPlaced:number; sold:number; missing:number; settled:number; latestNotes:string; latestAt:string|null };
export function partnerMetrics(p:RetailPartner,s:RetailSettings){const remaining=Math.max(0,p.stockPlaced-p.sold-p.missing);const sellThrough=p.stockPlaced?p.sold/p.stockPlaced*100:0;const revenue=p.sold*s.retail_price;const storeCommission=p.sold*s.commission;const fourRegnShare=p.sold*(s.retail_price-s.commission);const outstanding=Math.max(0,fourRegnShare-p.settled);const grossProfit=p.sold*(s.retail_price-s.commission-s.unit_cost);const shrinkage=p.stockPlaced?p.missing/p.stockPlaced*100:0;return{remaining,sellThrough,revenue,storeCommission,fourRegnShare,outstanding,grossProfit,shrinkage};}
export function decisionFor(m:ReturnType<typeof partnerMetrics>,s:RetailSettings){if(m.sellThrough>=s.scale_sell_through&&m.shrinkage<=s.max_shrinkage&&m.outstanding<=.01)return{key:"scale",label:"SCALE SIGNAL"};if(m.sellThrough>=s.minimum_sell_through&&m.shrinkage<=Math.max(5,s.max_shrinkage))return{key:"hold",label:"HOLD & PROVE"};return{key:"watch",label:"WATCH"};}
export function pilotTotals(ps:RetailPartner[],s:RetailSettings){return ps.reduce((a,p)=>{const m=partnerMetrics(p,s);a.stock+=p.stockPlaced;a.sold+=p.sold;a.remaining+=m.remaining;a.missing+=p.missing;a.revenue+=m.revenue;a.commission+=m.storeCommission;a.owed+=m.outstanding;a.settled+=p.settled;a.profit+=m.grossProfit;return a;},{stock:0,sold:0,remaining:0,missing:0,revenue:0,commission:0,owed:0,settled:0,profit:0});}
